   //====================================================================
   //== Read camac one 32 bit word
   //====================================================================
{/*
	long i, j, ndata;

	for(j = 20; j < 24; ++j)
	{
		*memory.p32++ = mwpc_ptr[j]->top; //header to output buffer
		
		
	   for(i = 0; i < 16; ++i)
	   {
		  if(mwpc_ptr[j]->k_read[i])
		  {
			*memory.p32++ = ((*mwpc_ptr[j]->data[i]) & 0xfff) + (mwpc_ptr[j]->adr[i]);
		  }
	   }
	   
	   *memory.p32++ = mwpc_ptr[j]->bot; //footer to output buffer
	}*/
}

// EOF